The overlay directory.
======================

This is where pre-configured files you want to lay down before running the server will go.

For example, we have a config file that turns off user authentication in the flags.